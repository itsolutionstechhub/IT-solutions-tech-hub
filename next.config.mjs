/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // We use standard <img> tags, so no remotePattern configuration is strictly required for <Image>.
};

export default nextConfig;
